package com.growthbuddy.reminder;

/**
 * Color tag for a calendar reminder. Constant names are intentionally lowercase
 * so they serialize to/from the exact strings the frontend uses.
 */
public enum ReminderTag {
    work, personal, health, urgent, other
}
